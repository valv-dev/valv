{
  "mcpServers": {
    "valv": {
      "command": "npx",
      "args": [
        "-y",
        "@valv/mcp"
      ],
      "env": {
        "DATABASE_URL": "postgresql://postgres:postgres@localhost:5432/mydb"
      }
    }
  }
}
