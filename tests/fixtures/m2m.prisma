generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Post {
  id   String @id @default(uuid())
  name String
  tags Tag[]
}

model Tag {
  id    String @id @default(uuid())
  name  String
  posts Post[]
}
